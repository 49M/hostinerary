[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_AUTH0_CLIENT_ID,VITE_AUTH0_DOMAIN"

# Proxy /api/* to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://hostinerary.onrender.com/api/:splat"
  status = 200
  force = true

# SPA fallback — send all non-file routes to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
